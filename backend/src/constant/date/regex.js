// Simplified regex for french date
// @todo: update all dates to ISO8601 date from data source and remove this logic
export const FR_DATE_EXTRACT_REGEX = /^(\d{2})\/(\d{2})\/(\d{4})/

// Simplified regex for ISO8601 date
// @todo: update all dates to ISO8601 date from data source and remove this logic
export const ISO_8601_DATE_EXTRACT_REGEX = /^(\d{4}-\d{2}-\d{2})/
